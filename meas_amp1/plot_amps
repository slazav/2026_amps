#!/usr/bin/gnuplot

set terminal png size 800,600
set output "plot_amps.png"

#set log y
set xlabel "R"
set ylabel "K"
set title ""
set key top right
set grid mxtics mytics xtics ytics
set log

K=100

f(x) = 1 + B/x

A=1
B=50e3
#fit f(x) "amps.txt" via B

plot [50:100e3] [1:1000]\
  "amps.txt" u 1:2 w p pt 7 notitle,\
  "amps.txt" u 1:3 w p pt 7 notitle,\
  f(x) lc -1 notitle,\

#  "amps_pl.txt" u 1:(f($1)) w p pt 7 notitle,\

print f(10e3), f(3.3e3), f(1e3), f(330), f(100)